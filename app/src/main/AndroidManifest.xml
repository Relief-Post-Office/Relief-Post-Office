<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.seoul42.relief_post_office">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"/>
    <uses-permission android:name="android.permission.CALL_PHONE" />
    <uses-permission android:name="android.permission.DIAL_PHONE" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.REQUEST_IGNORE_BATTERY_OPTIMIZATIONS"/>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:usesCleartextTraffic="true"
        android:theme="@style/Theme.Relief_post_office">
        <activity
            android:name=".service.CheckLoginService"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".MainActivity"
            android:windowSoftInputMode="adjustNothing"/>
        <activity android:name=".PhoneActivity"
            android:windowSoftInputMode="adjustNothing"/>
        <activity android:name=".JoinActivity"
            android:windowSoftInputMode="adjustNothing"/>
        <activity android:name=".guardian.MainFragment"
            android:windowSoftInputMode="adjustNothing"
            tools:ignore="Instantiatable" />
        <activity android:name=".GuardianBackgroundActivity"
            android:windowSoftInputMode="adjustNothing"/>
        <activity android:name=".ward.WardActivity"
            android:windowSoftInputMode="adjustNothing"/>
        <activity android:name=".alarm.AlarmActivity"
            android:windowSoftInputMode="adjustNothing"/>
        <activity android:name=".guardian.GuardianProfileActivity"
            android:windowSoftInputMode="adjustNothing" />
        <activity android:name=".ward.WardProfileActivity"
            android:windowSoftInputMode="adjustNothing" />

        <service
            android:name=".service.MyFirebaseMessagingService"
            android:exported="false">
            <intent-filter>
                <action android:name="com.google.firebase.MESSAGING_EVENT" />
            </intent-filter>
        </service>

    </application>

</manifest>